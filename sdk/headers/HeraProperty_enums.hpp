enum class EHeraComparisonType {
    EQUALS = 0,
    GREATER_THAN = 1,
    LESS_THAN_EQ = 2,
    EHeraComparisonType_MAX = 3,
};

enum class EHeraListComparisonType {
    CONTAINS = 0,
    EHeraListComparisonType_MAX = 1,
};

enum class EPropertyScopeType {
    Instance = 0,
    Session = 1,
    Transient = 2,
    Max = 3,
};

